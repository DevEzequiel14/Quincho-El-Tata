import { ScrollChangeDirective } from './scroll-change.directive';

describe('ScrollChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollChangeDirective();
    expect(directive).toBeTruthy();
  });
});
